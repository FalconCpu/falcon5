ld   $1, 5        # i = 5
ld   $2, 1        # constant 1

loop:
sub  $1, $1, $2   # i = i - 1
bge  $1, 0,  loop # if i >= 0 branch to loop

ld $3, 10         # some load operations after the loop to test pipeline flushing
ld $4, 20
ld $5, 30
ld $6, 40
