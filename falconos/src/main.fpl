
fun main()
    copyFontData()
    val gc = new GraphicsContext()
    gc.setup()
    gc.drawRect(0, 0, 640, 480, 0)       # Clear screen to black
    for i in 0..255
        val x = (i % 16) * 32
        val y = (i / 16) * 28
        gc.drawRect(x, y, x + 30, y + 26, i)  # Draw color blocks

    gc.drawText(10, 10, 15, "Hello, World!")  # Draw white text
    while true
        val a = 1


